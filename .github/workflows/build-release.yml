name: Build and Release

<<<<<<< HEAD
=======
permissions:
  contents: write

>>>>>>> fix-electron-builder-invalid-png-checksum
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest]
        
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Build SvelteKit app
        run: npm run build
        
      - name: Build Electron app (Windows)
        if: matrix.os == 'windows-latest'
        run: npm run electron:build:win
        
      - name: Build Electron app (macOS)
        if: matrix.os == 'macos-latest'
        run: npm run electron:build:mac
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false
        
      - name: Upload Windows artifacts
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v4
        with:
          name: windows-installers
          path: |
            release/*.exe
            
      - name: Upload macOS artifacts
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v4
        with:
          name: macos-installers
          path: |
            release/*.dmg
            release/*.zip
            
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          
<<<<<<< HEAD
      - name: List available artifacts
        run: |
          echo "Available artifacts:"
          find artifacts -name "*.exe" -o -name "*.dmg" -o -name "*.zip" | head -10
          
      - name: Create Release (Manual)
        run: |
          echo "Build completed successfully!"
          echo "Artifacts are available as GitHub Actions downloads."
          echo "Tag: ${{ github.ref_name }}"
          echo "Build time: $(date)"
          
      - name: Upload Release Assets
        if: false
        uses: actions/upload-artifact@v4
        with:
          name: release-assets
          path: |
            artifacts/windows-installers/*
            artifacts/macos-installers/*
=======
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/windows-installers/*
            artifacts/macos-installers/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
>>>>>>> fix-electron-builder-invalid-png-checksum
